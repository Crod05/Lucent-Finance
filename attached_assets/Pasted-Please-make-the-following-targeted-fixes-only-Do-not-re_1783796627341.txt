Please make the following targeted fixes only.
Do not rebuild unrelated systems.

1. Harden onboarding reset protection
- Treat reset as disabled by default.
- Only register or allow POST /gamification/onboarding/reset when
  NODE_ENV is exactly "development" or "test".
- In every other environment, return 403 or do not register the route.
- Keep the reset control hidden outside development/test builds.
- Add a test covering production, prod, Production, unset, staging,
  development, and test values.

2. Prepare and enforce transaction fingerprint uniqueness
- First inspect the current database for duplicate non-null fingerprints.
- If duplicates exist, report them and apply a deterministic,
  documented cleanup strategy before creating the unique constraint.
- Add a checked-in incremental migration that creates a unique index
  on non-null transaction fingerprints.
- Do not silently report success when a duplicate is rejected.
- Return an explicit 409 duplicate response, or return the existing
  transaction with a duplicate indicator.
- Confirm how fingerprints are generated.
- Recalculate the fingerprint when any fingerprint-producing field is
  updated.
- Ensure concurrent duplicate requests cannot insert two rows.
- Add tests for duplicate creates and updates.

3. Implement Budget Guardian with a meaningful rule
- Keep Budget Guardian as a one-time achievement badge.
- Define eligibility as:
  the player completed a full calendar month at or below every active
  monthly budget for that month.
- Do not award it merely because the current month has just started.
- Build a reusable budget-compliance evaluator that can evaluate a
  specified completed year/month.
- Grant the achievement idempotently through the existing achievement
  system.
- Do not create a repeatable monthly XP award in this task.
- Add tests showing:
  a completed compliant month earns the badge once;
  an over-budget month does not;
  repeated evaluation does not duplicate the badge.
- Document that refund/transfer semantics may affect this calculation
  until transaction semantics are formally defined.

4. Document the UTC limitation
- Add a clear comment near the date helpers explaining that all current
  gamification dates use UTC midnight rather than player-local time.
- Add the same limitation to replit.md.
- Do not claim timezone support is complete.

5. Temporary public-deployment warning
- Add a prominent note in replit.md that the live app remains publicly
  writable through the shared default-user and must not contain real
  personal financial data.
- Do not attempt full authentication in this task.

After implementation:
- Run typecheck and available tests.
- Apply the incremental migration against a copy of the current schema,
  not only a fresh empty database.
- Confirm the application starts.
- Push to Crod05/Lucent-Finance.
- Provide the commit hash, migration name, duplicate-data findings,
  and test results.